<%= stylesheet_link_tag "admin_panel" %>

<section>
  <div class="container mt-5 mb-5">
    <p>
      <strong>Title:</strong>
      <%= @free_video.title %>
    </p>

    <p>
      <strong>Url:</strong>
      <%= @free_video.url %>
    </p>

    <p>
      <strong>Description:</strong>
      <%= @free_video.description %>
    </p>

    <%= link_to 'Edit', edit_free_video_path(@free_video) %> |
    <%= link_to 'Back', free_videos_path %>
  </div>
</section>

